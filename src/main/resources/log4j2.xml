<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO" monitorInterval="30" shutdownHook="disable">
    <Appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <!-- serviceName must be equals to the repository-metadata-->
            <PatternLayout pattern="%clr{%d{yyyy-MM-dd HH:mm:ss.SSS}}{faint} %clr{%5p} %clr{${sys:PID}}{magenta} %clr{---}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{%X}{faint} %clr{:}{faint} %m%n%xwEx"> </PatternLayout>
        </Console>
    </Appenders>
    <Loggers>
        <!-- Default log level -->
        <Root level="DEBUG" additivity="false">
            <AppenderRef ref="CONSOLE"/>
        </Root>
    </Loggers>
</Configuration>